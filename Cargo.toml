[package]
name = "salyut-site"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "The salyut.one website"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
tiny_http = "0.12"

[profile.release]
lto = "thin"
strip = true
